# @package _global_

# SO(3) Varadhan DSM using the upstream wrapped-normal benchmark.
defaults:
  - so3
  - /teacher: varadhan
  - override /generator: lie_algebra
  - override /loss: dsmv

name: so3
experiment: so3

generated_samples_path: ${work_dir}/results/so3_varadhan/generated_samples.npy
