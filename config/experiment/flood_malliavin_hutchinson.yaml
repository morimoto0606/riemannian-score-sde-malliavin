# @package _global_

# Upstream Flood DSM with a Hutchinson Malliavin teacher target.
defaults:
  - earth_data
  - /dataset: flood
  - /teacher: malliavin_hutchinson

name: flood
experiment: flood

generated_samples_path: ${work_dir}/results/flood_malliavin_hutchinson/generated_samples.npy
