# @package _global_

# Upstream Earthquake DSM baseline with the existing Heat score target.
defaults:
  - earth_data
  - /dataset: earthquake
  - /teacher: heat

name: earthquake
experiment: earthquake

# This is the only operational addition to experiment=earthquake.  Network,
# loss, optimiser, EMA, data split, seed, and reverse sampler are inherited
# unchanged from the upstream configuration.
generated_samples_path: ${work_dir}/results/earthquake_upstream_heat_baseline/generated_samples.npy
