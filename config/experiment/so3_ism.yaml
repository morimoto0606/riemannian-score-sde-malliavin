# @package _global_

# SO(3) implicit score matching using the upstream wrapped-normal benchmark.
defaults:
  - so3
  - override /generator: lie_algebra
  - override /loss: ism

name: so3
experiment: so3

generated_samples_path: ${work_dir}/results/so3_ism/generated_samples.npy
