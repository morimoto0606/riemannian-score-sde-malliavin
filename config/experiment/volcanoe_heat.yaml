# @package _global_

# Upstream Volcano DSM baseline with the existing Heat score target.
defaults:
  - earth_data
  - /dataset: volcanoe
  - /teacher: heat

name: volcanoe
experiment: volcanoe

# This is the only operational addition to experiment=volcanoe.  Network,
# loss, optimiser, EMA, data split, seed, and reverse sampler are inherited
# unchanged from the upstream configuration.
generated_samples_path: ${work_dir}/results/volcanoe_upstream_heat_baseline/generated_samples.npy
