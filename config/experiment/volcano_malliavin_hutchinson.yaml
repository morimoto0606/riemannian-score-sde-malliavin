# @package _global_

# Upstream Volcano DSM with a Hutchinson Malliavin teacher target.
defaults:
  - earth_data
  - /dataset: volcano
  - /teacher: malliavin_hutchinson

name: volcano
experiment: volcano

generated_samples_path: ${work_dir}/results/volcano_malliavin_hutchinson/generated_samples.npy
