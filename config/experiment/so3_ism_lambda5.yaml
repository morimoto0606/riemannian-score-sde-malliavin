# @package _global_

# SO(3) implicit score matching with native like_w followed independently by
# the additional exp(-5 t) experiment weighting.
defaults:
  - so3
  - override /generator: lie_algebra
  - override /loss: ism

name: so3_ism_lambda5
experiment: so3_ism
generated_samples_path: ${work_dir}/results/so3_ism_lambda5/generated_samples.npy

loss:
  like_w: true
  time_weighting: true
  time_weight_lambda: 5.0
