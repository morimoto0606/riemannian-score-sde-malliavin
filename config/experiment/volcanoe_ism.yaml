# @package _global_

# Volcano implicit score matching with the upstream Earth-data settings.
defaults:
  - earth_data
  - /dataset: volcanoe
  - override /loss: ism

name: volcanoe
experiment: volcanoe

generated_samples_path: ${work_dir}/results/volcanoe_ism/generated_samples.npy
