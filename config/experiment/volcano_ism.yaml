# @package _global_

# Volcano implicit score matching with the upstream Earth-data settings.
defaults:
  - earth_data
  - /dataset: volcano
  - override /loss: ism

name: volcano
experiment: volcano

generated_samples_path: ${work_dir}/results/volcano_ism/generated_samples.npy
