# @package _global_

# Upstream Earthquake DSM with only the conditional score teacher replaced.
defaults:
  - earth_data
  - /dataset: earthquake
  - /teacher: malliavin

name: earthquake
experiment: earthquake

generated_samples_path: ${work_dir}/results/earthquake_malliavin/generated_samples.npy
