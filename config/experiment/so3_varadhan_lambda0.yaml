# @package _global_

# SO(3) upstream Varadhan DSM baseline without additional time reweighting.
defaults:
  - so3
  - override /generator: lie_algebra
  - override /loss: dsmv
  - /teacher: varadhan

name: so3_varadhan_lambda0
experiment: so3_varadhan
generated_samples_path: ${work_dir}/results/so3_varadhan_lambda0/generated_samples.npy

loss:
  time_weighting: true
  time_weight_lambda: 0.0
